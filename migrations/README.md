# Database migrations

PostgreSQL migrations will live here. The initial server does not connect to PostgreSQL yet; mutable forge state is the next implementation slice after the HTTP and local storage boundaries.
